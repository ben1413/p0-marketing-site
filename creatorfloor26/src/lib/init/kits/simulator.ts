export const SIMULATOR_KIT_CONTEXT = `
CreatorKit: Simulator. Click/collect loop, multipliers, pets, rebirth layers, egg RNG,
trading risks. Hook into retention metrics (session length, return visits).
`.trim();
