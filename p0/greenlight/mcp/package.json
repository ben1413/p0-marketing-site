{
  "name": "@greenlight/mcp",
  "version": "0.1.0",
  "description": "Greenlight MCP — governed live ops pipeline for AI agents. Proposals, simulation, deploy, rollback, moderation, incidents, experiments.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.0"
  }
}
